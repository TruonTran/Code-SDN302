# Khởi tạo npm:

npm init -y

## Cài Express:

npm install express

npm install express mongoose dotenv
npm install --save-dev nodemon
npm install cors

npm install
npm start

npm install mongodb
npm install mongoose

## Cài đặt dotenv

npm install dotenv

## Chạy dự án

npm run dev.

### code cai ejs

npm install express mongoose ejs body-parser method-override
npm install --save-dev nodemon

### handlebars

npm install express express-handlebars

npm install express express-handlebars handlebars uuid

### Thêm connect-flash và express-session cho thông báo

npm install express express-handlebars mongoose connect-flash express-session

### gom lại cài cho nhanh nè
npm install express mongoose dotenv morgan helmet cors
npm install passport passport-local passport-local-mongoose express-session
npm install jsonwebtoken bcryptjs

### cái passport local
npm install express passport passport-local
npm install express mongoose passport passport-local bcryptjs

### assignment 2
npm install express ejs express-handlebars body-parser method-override axios bootstrap 

### cách tạo https 
mở git bash dùng
mkdir -p cert openssl req -x509 -newkey rsa:2048 -nodes -keyout cert/key.pem -out cert/cert.pem -days 365



```(Thêm uuid để tạo ID duy nhất cho nhiệm vụ mà không cần DB).

git init
git remote add origin https://github.com/TruonTran/Code-SDN302.git
git branch -M main
git add .
git commit -m "first commit"
git push -u origin main

```
