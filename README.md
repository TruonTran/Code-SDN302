# Khởi tạo npm:
npm init -y

## Cài Express: 
npm install express

npm install express mongoose dotenv
npm install --save-dev nodemon 

npm install
npm start

npm install mongodb
npm install mongoose

## Cài đặt dotenv
npm install dotenv

## Chạy dự án
npm run dev.

### code cai ejs
npm install express mongoose ejs body-parser method-override
npm install --save-dev nodemon

### handlebars
npm install express express-handlebars

npm install express express-handlebars handlebars uuid
```(Thêm uuid để tạo ID duy nhất cho nhiệm vụ mà không cần DB).

git init
git branch -M main
git add .
git commit -m "first commit"
git push -u origin main

